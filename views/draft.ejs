<!DOCTYPE html>
<html lang="en" data-bs-theme="light">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/styles/draft.css">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link rel="stylesheet" href="/bootstrap-icons/bootstrap-icons.css">
        <link
            href="https://fonts.googleapis.com/css2?family=IM+Fell+DW+Pica:ital@0;1&display=swap"
            rel="stylesheet">
        <title>Draft A New Letter</title>
    </head>

    <body>
        <div class="background">
            <div class="portal">
                <h1>Draft A New Letter</h1>
                <div class="transbox">
                    <form action="/draft" method="POST">
                        <%# When the form is submitted, the browser sends a POST
                        request to /draft, with the form data in the request
                        body. %>
                        <div class="form-floating up">
                            <label for="title">Title:</label>
                            <input type="text" id="title" name="title"
                                required>
                        </div>
                        <div class="form-floating middle">
                            <label for="author">Author:</label>
                            <input type="text" id="author" name="author"
                                required>
                        </div>
                        <div class="form-floating down">
                            <label for="content">Content:</label>
                            <textarea id="content" name="content" rows="8"
                                cols="40"
                                maxlength="500" required>
                                </textarea>
                        </div>
                        <button class="btn"
                            type="submit">Confirm</button>
                    </form>
                </div>
            </div>
        </div>
    </body>
</html>